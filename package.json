{
  "name": "agr-custom-modal",
  "version": "1.3.0",
  "description": "A fully-featured and customizable Modal component for React, with support for title, content, and custom actions.",
  "main": "./dist/Modal.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel Modal.tsx --out-dir dist --extensions \".tsx\" --source-maps inline && cp Modal.css dist/Modal.css",
    "publish": "npm run build && npm publish --access=public",
    "publish:git": "npm run build && npm publish --access=public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GrimonprezAlexis/react-custom-modal.git"
  },
  "keywords": [
    "React",
    "Custom",
    "Modal"
  ],
  "author": "AGR",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/GrimonprezAlexis/react-custom-modal/issues"
  },
  "homepage": "https://github.com/GrimonprezAlexis/react-custom-modal#readme",
  "devDependencies": {
    "@babel/preset-env": "^7.20.2",
    "@babel/preset-react": "^7.18.6",
    "@babel/preset-typescript": "^7.18.6"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
